[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "frontcache"
version = "0.1.0"
description = "Python bindings for FrontCache distributed cache client"
requires-python = ">=3.8"

[tool.maturin]
module-name = "frontcache"
